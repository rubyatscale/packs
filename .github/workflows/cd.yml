name: CD

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]
jobs:
  cd:
    uses: rubyatscale/ruby-gem-ci-and-cd-action/.github/actions/cd.yml@main
    secrets: inherit
